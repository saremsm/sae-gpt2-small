torch>=2.0.0
transformer_lens>=1.14.0
datasets>=2.14.0
# data.py tokenizes with transformers' GPT-2 tokenizer directly (no model
# load); it is already a transformer_lens dependency.
transformers>=4.30.0
# monology/pile-uncopyrighted streams .jsonl.zst shards; datasets needs
# this to decompress them and does not pull it in itself.
zstandard>=0.21
# torch.cuda.utilization() (bench_pipeline.py's GPU-utilization gate)
# needs the pynvml module; nvidia-ml-py is the tiny official package.
nvidia-ml-py>=12.0
tqdm>=4.65.0
matplotlib>=3.7.0
numpy>=1.24.0,<2
pytest>=7.0.0

# Transitive deps that must come from the venv, not Ubuntu's apt packages.
# Lambda images expose system scipy/pandas/sklearn/pillow built against
# NumPy 1.x and an old PIL; venv-native builds avoid the ABI mismatch.
scipy>=1.11
pandas>=2.0
scikit-learn>=1.3
networkx>=3.0
pillow>=9.1
